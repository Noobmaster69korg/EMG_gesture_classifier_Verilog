This basically contains a google collab notebook where; I had written codes to see from the software what should had been the values of inference at various stages.
While running the behavioural simulation in vivado, the final result wasnt matching.

This software simulation helped in identifying at which stage of the inference was the error coming.
NOTE: This is NOT the google colab file where the model was ACTUALLY DESIGNED. That has an another github repo created by Hrishi 
